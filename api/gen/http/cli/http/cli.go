// Code generated by goa v3.6.0, DO NOT EDIT.
//
// http HTTP client CLI support package
//
// Command:
// $ goa gen CryptoStats/api/design

package cli

import (
	ratec "CryptoStats/api/gen/http/rate/client"
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `rate rate
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` rate rate` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		rateFlags = flag.NewFlagSet("rate", flag.ContinueOnError)

		rateRateFlags = flag.NewFlagSet("rate", flag.ExitOnError)
	)
	rateFlags.Usage = rateUsage
	rateRateFlags.Usage = rateRateUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "rate":
			svcf = rateFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "rate":
			switch epn {
			case "rate":
				epf = rateRateFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "rate":
			c := ratec.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "rate":
				endpoint = c.Rate()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// rateUsage displays the usage of the rate command and its subcommands.
func rateUsage() {
	fmt.Fprintf(os.Stderr, `The rate service provides BTC to UAH exchange rate
Usage:
    %[1]s [globalflags] rate COMMAND [flags]

COMMAND:
    rate: This request should return the current BTC to UAH exchange rate using any third-party service with a public API

Additional help:
    %[1]s rate COMMAND --help
`, os.Args[0])
}
func rateRateUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] rate rate

This request should return the current BTC to UAH exchange rate using any third-party service with a public API

Example:
    %[1]s rate rate
`, os.Args[0])
}
